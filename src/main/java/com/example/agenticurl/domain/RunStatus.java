package com.example.agenticurl.domain;

public enum RunStatus {
    CREATED,
    RUNNING,
    AWAITING_APPROVAL,
    AWAITING_CLARIFICATION,
    REPLANNING,
    COMPLETED,
    FAILED,
    STOPPED,
    ROLLED_BACK
}
